'use client';

/**
 * @deprecated Play sidebar has been replaced by the desktop slim top navigation
 * and the mobile persistent bottom tab bar. This export is retained as an empty
 * component to prevent breaking any legacy or external references.
 */
export function Sidebar() {
  return null;
}

export default Sidebar;
